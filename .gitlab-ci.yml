image: ruby:2.4

variables:
  JEKYLL_ENV: production
  LC_ALL: C.UTF-8

#before_script:
#  - bundle install

pages:
  stage: deploy
  script:
  - gem install bundler
  - gem install jekyll
  - bundle exec jekyll build -d _site
  artifacts:
    paths:
    - _site
  only:
    - master
